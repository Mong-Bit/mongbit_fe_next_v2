export type url = {
  url: string;
};

export type updateLogInStateProp = {
  dataList: {
    memberId: string;
    thumbnail: string;
    registDate: string;
    username: string;
  };
  headers: Headers;
};
